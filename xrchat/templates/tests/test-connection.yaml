apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xrchat.fullname" . }}-test-connection"
  labels:
    {{- include "xrchat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xrchat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
