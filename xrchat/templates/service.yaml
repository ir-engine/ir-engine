apiVersion: v1
kind: Service
metadata:
  name: {{ include "xrchat.fullname" . }}
  labels:
    {{- include "xrchat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xrchat.selectorLabels" . | nindent 4 }}
