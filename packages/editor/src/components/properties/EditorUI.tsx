import React from 'react'

// This will be Editor Input UI which will use component schema to render the inputs
export default class EditorUI extends React.Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            <div>Hello world</div>
        )
    }
}