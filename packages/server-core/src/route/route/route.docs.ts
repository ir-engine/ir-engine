export default {
  definitions: {
    project: {
      type: 'string',
      description: 'The project to add routes from'
    },
    route: {
      type: 'string',
      description: 'The route to enable for this project'
    }
  }
}
