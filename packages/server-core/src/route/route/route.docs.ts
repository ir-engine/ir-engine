export default {
  definitions: {
    project: {
      type: 'string',
      description: 'The project to add routes from'
    },
    routes: {
      type: 'string',
      description: 'The routes to enable for this project'
    }
  }
}
