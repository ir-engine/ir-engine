describe('Vector Animation Movement', () => {
  it("no move", () => {
  });
  it("forward", () => {
  });

  it("forward run", () => {
  });

  it("forward mid walk to run", () => {
  });

  it("backward left mid walk to run", () => {
  });
});