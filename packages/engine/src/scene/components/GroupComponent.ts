import { createMappedComponent } from '../../ecs/functions/ComponentFunctions'

export const GroupComponent = createMappedComponent<true>('GroupComponent')

export const SCENE_COMPONENT_GROUP = 'group'