import { Vector3 } from 'three'
export default function computeDistanceFromBoundingCircle(
  viewerPosition: Vector3,
  centerPoint: [number, number],
  boundingCircleRadius: number
) {
  // TODO
  return 5
}
