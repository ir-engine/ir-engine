import { Component } from "ecsy";
interface PropTypes {
    networkId: number;
    userId: string;
    name: string;
}
export default class NetworkPlayer extends Component<PropTypes> {
}
export {};
